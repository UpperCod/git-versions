name: Mi Flujo de Trabajo

on: push

jobs:
  verificar-latest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node index.mjs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.ref }}
